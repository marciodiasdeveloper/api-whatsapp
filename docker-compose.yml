version: "3"

services:
  app:
    container_name: api-whatsapp
    build: .
    env_file:
      - .env
    restart: always
    ports:
      - 3010:3010
    volumes:
      - .:/usr/app

volumes:
  pgdata:
    driver: local
